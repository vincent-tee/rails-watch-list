<%= render "nav", list: @list %>

<div class="container">
  <div class="row">
    <h1>Create a new List</h1>
    <div class="col">
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.submit %>
    <% end %>
    </div>
  </div>
</div>
